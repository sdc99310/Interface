function setup() {
  createCanvas(windowWidth, windowHeight);
  background(350);
}

function draw(){
  if (mouseIsPressed) {
    stroke(255);

    let d = dist(mouseX, mouseY, pmouseX, mouseY);
  }



}

  